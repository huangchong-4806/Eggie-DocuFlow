# Eggie DocuFlow V1.4.0 PRD

## 一、版本定位

V1.4.0 是一次性综合升级版，继续保持“轻量办公文件处理工具”的定位。

本版本围绕四件事展开：

1. 让软件内部更容易继续开发，减少修改一个功能影响其他页面的风险。
2. 增加批量处理中心，让用户可以一次处理整个文件夹中的 PDF。
3. 增加 Excel 数据清理和智能文件命名，减少重复手工操作。
4. 增强 PDF 工具箱，补齐水印、页码、密码、可搜索 PDF 和文字对比。

本版本不把软件改造成台账、报销、协作或云端管理系统。

## 二、已确认范围

### 1. 软件内部整理

1. 保留现有功能和现有蓝色毛玻璃界面。
2. 新功能优先复用现有文件读取、OCR、日志、后台处理和安全保存能力。
3. 逐步减少主窗口承担的具体处理工作，新功能的文件处理规则放在独立文件中。
4. 百度和阿里 OCR 只保留一套正式处理入口，避免后续出现两套规则结果不一致。
5. 所有文件处理均先生成完整的新文件，再公布为正式结果，失败时不留下半成品。
6. 所有工具统一显示进度、成功数量、失败原因、保存位置和打开结果入口。

### 2. 批量处理中心

用户可以选择一个文件夹，批量处理其中的 PDF 文件。

必须支持：

1. 可选择是否包含子文件夹。
2. 开始前显示发现的 PDF 数量、文件名和预计处理方式。
3. 自动判断发票、合同、表格或普通文档。
4. 文本型 PDF 默认只在本机处理。
5. 扫描 PDF 只有在用户主动勾选云 OCR、已配置密钥并再次确认后才允许上传页面。
6. 每个文件单独显示等待、处理中、成功或失败。
7. 一个文件失败不影响其他文件。
8. 完成后可以只重试失败文件。
9. 结果统一保存到用户选择的文件夹，并生成包含关键字段的处理日志。
10. 不保存业务状态，不形成长期台账。

### 3. Excel 数据清理

用户选择一个 `.xlsx` 或 `.xlsm` 文件和一个工作表，在开始前查看预计修改数量。

必须支持：

1. 删除整行都为空的空白行。
2. 清理文字开头和结尾的多余空格，并压缩连续普通空格。
3. 按用户勾选的列排重；未勾选列时按整行排重。
4. 表头行由用户指定，默认第 1 行。
5. 日期统一只调整已有日期单元格的显示方式，不猜测普通文字是不是日期。
6. 数字统一只调整已有数字单元格的显示方式，不改变数值。
7. 预览显示原始行数、空白行数、重复行数、空格问题数量、日期数量和数字数量。
8. 永远保存为新文件，不覆盖源文件。
9. 输出详细日志，记录工作表、规则、处理前后数量和结果路径。
10. 保留其他工作表；只修改用户选中的工作表。

安全边界：

1. 不自动删除空白列，避免破坏表格布局。
2. 不自动把文字转成数字或日期，避免改变编号、身份证号和前导零。
3. 如果工作表含公式，界面必须提醒用户检查结果。

### 4. 智能文件命名和重复文件检查

智能命名放在现有“批量改名”工具中，不新增一个重复入口。

必须支持：

1. 普通规则改名继续保留。
2. 新增“智能识别命名”模式，只处理 PDF。
3. 发票默认建议名称：开票日期、销售方简称、价税合计。
4. 合同默认建议名称：合同标题、识别到的首个日期。
5. 无法可靠识别时保持原文件名，并显示原因。
6. 智能命名必须先预览，用户确认后才能执行。
7. 自动清理文件名中系统不允许使用的符号。
8. 遇到空名称、重名或目标已存在时禁止执行。
9. 重复文件按文件内容计算，不只看文件名和大小。
10. 重复文件只标记，不自动删除或移动。
11. 扫描 PDF 需要云 OCR 时遵守与批量处理中心相同的用户确认规则。

### 5. PDF 工具箱增强

新增能力继续放在现有 PDF 工具箱内，不增加多个首页入口。

#### 5.1 水印和页码

1. 支持添加文字水印。
2. 支持设置水印文字、透明度、角度、字号和位置。
3. 支持添加页码。
4. 支持页码从指定数字开始。
5. 支持页码显示在顶部或底部、左侧或右侧。
6. 可同时添加水印和页码。
7. 永远生成新 PDF。

#### 5.2 PDF 密码

1. 支持给 PDF 设置打开密码。
2. 支持用户输入正确原密码后生成无密码副本。
3. 密码只在当前操作过程中使用，不写入日志、不保存到配置文件。
4. 密码错误时不生成结果文件。

#### 5.3 可搜索 PDF

1. 文本型 PDF 直接保留，不重复调用 OCR。
2. 扫描 PDF 使用用户已配置的百度或阿里 OCR。
3. 保留原页面图像，在页面上加入不可见文字层，使结果可以搜索和复制。
4. OCR 前必须明确显示会上传哪些页面。
5. 失败页面写入日志，不生成冒充完整结果的文件。

#### 5.4 PDF 文字对比

1. 用户选择两份 PDF。
2. 读取可复制文字并按行比较。
3. 输出本机可打开的 HTML 对比报告。
4. 报告显示新增、删除和未变化内容。
5. 扫描 PDF 不自动调用 OCR；需要 OCR 时先由用户转成可搜索 PDF。
6. 不上传 PDF 内容。

## 三、页面结构

左侧栏顺序：

1. 工作台
2. Excel 合并
3. Excel 拆分
4. Excel 数据清理
5. 发票解析
6. 文档处理
7. 批量处理
8. 批量改名
9. PDF 工具箱
10. 软件设置

首页使用八个主要工具卡片：

1. Excel 合并
2. Excel 拆分
3. Excel 数据清理
4. 发票解析
5. 文档处理
6. 批量处理
7. 批量改名
8. PDF 工具箱

PDF 工具箱使用标签页：

1. 页面整理
2. PDF 压缩
3. 图片与 PDF 互转
4. 水印与页码
5. PDF 密码
6. 可搜索 PDF
7. 文字对比

所有新页面继续使用现有蓝色毛玻璃背景、系统勾选框、统一按钮和统一文字层级。

## 四、用户操作原则

每个工具统一遵循：

1. 选择文件或文件夹。
2. 设置规则。
3. 查看预览或预计结果。
4. 点击开始。
5. 查看每个文件的成功或失败状态。
6. 打开结果文件或结果文件夹。

所有危险操作必须满足：

1. 不覆盖原文件。
2. 不静默上传文件。
3. 不保存 PDF 密码。
4. 不自动删除重复文件。
5. 用户取消或处理失败时清理半成品。

## 五、不做范围

V1.4.0 不做：

1. 发票台账中心、报销、付款、入账和税务申报。
2. 账号登录、多人协作和云端同步。
3. 自动删除或移动重复文件。
4. 自动覆盖原始文件。
5. 通用 PDF 内容编辑器。
6. 任意格式互相转换。
7. 聊天式 AI 助手。
8. 新增大量 OCR 服务商。
9. 本地离线 OCR 大型模型。
10. 没有用户提供原密码时破解加密 PDF。

## 六、详细执行计划

### 阶段 0：冻结基线

1. 记录当前分支和未完成改动。
2. 运行现有全部测试。
3. 确认 V1.3.13 的八个现有页面和主要功能可用。
4. 保存当前界面截图作为对照。

验收：现有 120 项测试通过，已有改动不被覆盖。

### 阶段 1：公共能力整理

1. 统一文件处理结果的成功、失败和日志字段。
2. 复用现有后台处理能力，避免界面卡住。
3. 确认 OCR 正式入口，停止新功能继续调用重复的旧入口。
4. 新功能的处理规则全部放在独立文件，主窗口只负责页面和按钮。

验收：现有功能测试全部通过，界面行为不变。

### 阶段 2：批量处理中心

1. 新增批量处理后端。
2. 新增批量处理页面和导航入口。
3. 实现文件夹扫描、预览、分类、逐个状态、失败继续和失败重试。
4. 接入本机处理和可选云 OCR。
5. 补充测试与处理日志。

验收：文本 PDF 批量成功；单个坏文件不影响其他文件；扫描 PDF 未确认时不上传。

### 阶段 3：Excel 数据清理

1. 新增清理规则和预览计算。
2. 新增工作表、表头和排重列选择。
3. 实现新文件输出和详细日志。
4. 新增页面、导航和首页卡片。
5. 使用 Microsoft Excel 真实打开输出文件。

验收：源文件不变；预览数量和实际结果一致；Excel 打开时没有损坏或修复提示。

### 阶段 4：智能命名和重复检查

1. 在批量改名中加入智能识别模式。
2. 复用现有发票和合同识别结果生成建议名称。
3. 加入文件内容重复检查。
4. 保持现有预览、冲突阻止和日志规则。

验收：不能识别时不改名；重复文件只标记；任何冲突都会阻止执行。

### 阶段 5：PDF 工具箱增强

1. 实现水印和页码。
2. 实现设置密码和已知密码移除。
3. 实现 OCR 可搜索 PDF。
4. 实现 PDF 文字对比 HTML 报告。
5. 新增 PDF 工具箱标签页和测试。

验收：原 PDF 不变；密码不进入日志；文字对比报告可打开；可搜索 PDF 可以搜索文字。

### 阶段 6：全量界面与质量检查

1. 检查首页、侧栏和全部工具页没有重叠。
2. 检查小窗口和常用分辨率。
3. 检查 Logo、勾选框、按钮、进度和提示统一。
4. 完成代码审查，修复高风险问题。
5. 运行全部自动检查和实际文件测试。
6. 更新 README、版本记录和界面截图。

### 阶段 7：打包发布文件

1. 版本统一更新为 V1.4.0。
2. 打包 macOS 正式 ZIP。
3. 通过 GitHub 在线 Windows 环境打包 Windows 10/11 64 位安装程序。
4. 验证签名、ZIP 完整性、Mac 软件启动、Windows 安装和版本运行。
5. 检查安装包不包含密钥、`.env`、缓存、测试文件和临时文件。
6. 输出 Mac 和 Windows 发布文件的 SHA-256 校验值。

## 七、测试清单

必须完成：

1. 所有原有自动测试。
2. 批量处理正常文件、损坏文件和混合文件测试。
3. OCR 未配置、配置错误、用户取消和网络失败测试。
4. Excel 空白行、空格、排重、日期、数字、公式和多工作表测试。
5. 智能命名成功、无法识别、重名和重复内容测试。
6. PDF 水印、页码、密码、可搜索 PDF 和文字对比测试。
7. 所有页面打开、导航和按钮状态测试。
8. macOS 打包版启动测试。
9. Windows 安装程序在真实 Windows 环境完成安装和版本运行测试。
10. 所有生成文件实际打开测试。
11. Excel 结果必须用 Microsoft Excel 客户端真实打开，不能出现损坏或修复提示。

## 八、最终交付

1. V1.4.0 源代码改动。
2. 更新后的 `PRD.md`。
3. 更新后的 `README.md` 和 `release_notes_v1.4.0.md`。
4. 全部主要页面截图。
5. 测试与代码审查结果。
6. `EggieDocuFlow_V1.4.0_mac.zip`。
7. `EggieDocuFlow_V1.4.0_Windows_x64_Setup.exe`。
8. Mac 和 Windows 发布文件的 SHA-256 校验值。
